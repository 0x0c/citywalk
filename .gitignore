# The roadmap site is a build artifact, regenerated from roadmaps/ on every build.
/site/

__pycache__/
*.pyc

# Protobuf codegen plugins built by `make tools`; rebuilt from the go.mod tool block on demand.
/.tools/

# Coverage profile written by `make test-unit`.
/coverage.out

# Scratch worktrees the agent harness creates for background subagents.
/.claude/worktrees/
